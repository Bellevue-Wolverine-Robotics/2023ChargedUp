package frc.robot.Istream;

/*
 * some kind of plan i guess to have multiple input methods be easily switched between (xbox and joystick)
 * so ig we can have like wrappers around the joystick input method and the xbox inptu method
 * 
 */
public interface IStream {
    
}
